# CLAUDE.md

> **规则：本地 commit 必须经我确认后才能执行。确认后自动推送到远端仓库。**

## Project Overview

GameFramework — 纯 .NET 核心库，无 Unity API，编译为 `GameFramework.asmdef`。

此仓库是 UnityGameFramework 的 submodule，修改后需要在此 commit → push。
